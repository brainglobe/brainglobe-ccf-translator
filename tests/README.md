to run the tests run
```bash
python -m unittest discover -s tests -v
```
from the root of the repository

to run just one test we can go
```bash
python -m unittest discover -s tests -p "test_space_utils.py" -v
